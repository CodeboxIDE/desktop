define(function() {
    return {
        name: "Chrome",

        // Only remote boxes on chrome
        allowOpenLocal: false,

        // When app is ready
        ready: function() {

        }
    };
});