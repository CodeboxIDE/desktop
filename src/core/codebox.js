define([], function() {
    // Open a local box
    var open = function(project) {

    };

    // Open a remote box
    var openRemote = function(project) {

    };

    return {
        'local': open,
        'remote': openRemote
    };
});